
# Neural Network with tanh Activation

This repository contains a Python program
that implements a simple neural network
with a tanh activation function.

- Weights are chosen randomly from [-0.5, 0.5].
- b1 = 0.5, b2 = 0.7.
- The final output of the network is printed.
